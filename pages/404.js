import { Typography } from "antd";
import React from "react";

const PagenotFound = () => {
  return (
    <div
      className="flex justify-center items-center"
      style={{ height: "100vh" }}
    >
      <Typography.Title level={5}>404 | Page not found</Typography.Title>
    </div>
  );
};

export default PagenotFound;
